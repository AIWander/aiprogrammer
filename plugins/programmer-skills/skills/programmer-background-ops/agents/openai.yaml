interface:
  display_name: "Programmer Background Ops"
  short_description: "State-carrying sessions, live REPL shells, and WSL jobs"
  default_prompt: "Use $programmer-background-ops for work that outlives one tool call or spans Windows and WSL."

policy:
  allow_implicit_invocation: true
